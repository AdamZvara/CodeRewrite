- perhaps move the post_eval into a single directory somewhere
    - right now we have 2 post evals at root and at src/lib/post_eval

- maybe rename the configurations so that they match the thesis ones?

- redo READMEs/docs ... make sure that mlflow export works and reuses some env variables

- make sure that everything is runnable + include some solutions for problems I've had and are on confluence

- add a license? check if easyedit has some license 